#
# ccore image load
#

comment "ccore image load driver"

config IMAGE_LOAD
	tristate "ccore image load"
	default n
	help
	  ccore image load driver

config COLD_PATCH
	tristate "ccore patch image load"
	default n
	help
	  ccore patch image load driver